({
    calculateDiscount: function(basePrice, discountRate) {
        return basePrice * (1 - discountRate);
    },
    
    logHello: function() {
        return "Hello dynamically from Git!";
    }
})
